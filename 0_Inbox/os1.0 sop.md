数据流进来log ，记录文本→ 
审计阶段：（我）先打引用标签，审计意见文本字段。路由就是标签：domain 到 一级对象 再到二级对象。
比如健康-补剂-肌酸
`文件夹3_Domains` -> `健康` -> `锻炼` -> `有氧` -> `跑步.md`。
想实现只打具体标签，而是自动补全前面的标签
→ 标签路由到obsidian文件夹里的具体文档。
→ 某个 control 对象 → 调用既定service算法 ，策略→ 输出动作或忽略

注意这一步的残酷之处：  
**如果没有匹配的 control，对不起，什么都不做。**

控制层的判断逻辑应该是：  
**不管出现什么新情况，只要满足某几个标签，就走同一条死板流程。**

怎么设计标签阶段后流程sop

2、只有行动需要完成时间（要么在表格里用筛选多加视图，（好处是在一个表也没，切换方便。）要么加入一个专门的action表（关于行动后续可能需要扩展维度，比如优先级，完成情况）。直接看别人的任务管理系统


最好的思想：时间流程，能量低或者来不及只有log，写文本。
第二阶段标签处理 